spring:
  application:
    name: "Spring-Security-OAuth"
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: github-client-id
            clientSecret: github-client-secret